# 채널 대시보드 (공개용)

이 폴더는 **GitHub Pages 배포 전용**입니다. 비밀값(API 키·토큰)은 들어있지 않습니다.

- `index.html` : 대시보드 화면
- `data/*.csv` : 수집된 데이터 (호스트 PC에서 하루 2회 자동 갱신·push)

> ⚠️ 이 폴더에는 절대 `.env`나 `collector/`를 넣지 마세요. (비밀값 노출 방지)
> 원본 프로젝트(수집기 포함)는 상위 폴더에 있습니다.
